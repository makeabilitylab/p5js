# .github/workflows/build-gallery.yml
#
# Auto-generate the example gallery (index.html) and deploy to GitHub Pages
# whenever code is pushed to the master branch.
#
# The workflow:
#   1. Checks out the repo
#   2. Runs scripts/build_gallery.py to generate index.html
#   3. Deploys the entire repo (including the new index.html) to GitHub Pages

name: Build Gallery & Deploy

on:
  push:
    branches: [master]
  workflow_dispatch:        # Allow manual trigger from the Actions tab

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Generate gallery index.html
        run: python scripts/build_gallery.py

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload the entire repo root so all example folders are served
          path: "."

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4